import React from 'react';

import { AmountText, ContentInfo } from './TransactionStyles';

const TransactionWithdrawPending = () => {
  return (
      <ContentInfo>
        Withdrawing all <AmountText>Air-HODLed DAPP</AmountText> tokens
      </ContentInfo>
  )
};

export default TransactionWithdrawPending;
