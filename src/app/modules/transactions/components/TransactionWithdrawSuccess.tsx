import React from 'react';

import { AmountText, ContentInfo } from './TransactionStyles';

const TransactionWithdrawSuccess = () => {
  return (
      <ContentInfo>
        Withdrew all <AmountText>Air-HODLed DAPP</AmountText> tokens
      </ContentInfo>
  )
};

export default TransactionWithdrawSuccess;
